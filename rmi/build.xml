<project
    name="RMI apps"
    default="build-app"
    basedir=".">

    <property name="user-classpath" value="external/ibis-poolinfo-2.2.jar"/>

    <import file="build-rmi-app.xml"/>

    <target name="build-app" depends="jar">
    </target>

</project>
